[package]
name = "memory-measure"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.42"
sysinfo = "0.36.1"

[profile.release]
strip = true  # Automatically strip symbols from the binary.
opt-level = "z"  # Optimize for size.
lto = true
codegen-units = 1
panic = "abort"
